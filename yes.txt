import { Inter } from "next/font/google";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
});




<main className={`font-sans ${inter.variable}`}>
      <Component {...pageProps} />
    </main>
